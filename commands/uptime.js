module.exports = {
    name: 'uptime',
    description: 'Displays how long the bot has been up and running',
    execute(message, args) {
        uptimeCommand(message, args);
    }
}

const uptimeCommand = (message, args) => {
    
}